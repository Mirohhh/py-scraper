[project]
name = "py-scraper"
version = "0.1.0"
description = "Web scraper with Scratch-style command builder UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "playwright>=1.40",
]
